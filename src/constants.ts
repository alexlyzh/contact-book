export enum AppRoute {
  Root = '/',
  Login = '/login',
  Contacts = '/contacts',
}

export enum InputType  {
  Name = 'text',
  Username = 'text',
  Email = 'email',
  Phone = 'tel',
  Website = 'text',
}
